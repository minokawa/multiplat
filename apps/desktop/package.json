{
  "name": "@litlag/desktop",
  "version": "0.0.1",
  "private": true,
  "description": "electron desktop app",
  "main": "./dist/index.js",
  "scripts": {
    "pack": "./do.sh -a package",
    "build": "./do.sh -a build",
    "launch": "npx electron ./dist/index.js",
    "start": "npm run build && npm run launch",
    "watch": "nodemon --config ./conf_nodemon.json --exec npm run start"
  }
}
